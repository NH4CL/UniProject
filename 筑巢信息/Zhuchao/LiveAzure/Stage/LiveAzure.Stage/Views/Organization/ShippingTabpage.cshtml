@using MVC.Controls;
@using MVC.Controls.Tab;


@Html.Tab( new TabControl()
    .AddTabItem(new TabItem()
        .SetSelected()
        .SetContent(MVC.Controls.ContentType.RemoteAction, "ShippingDetail")
        .SetTitle("ShippingDetail"))    
        )